#include <stdio.h>

/**
 * main - entry point of program
 *
 * description - prints 0 to 9 with new line
 *
 * return - returns zero
 */

int main(void)

{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	return (0);

}
